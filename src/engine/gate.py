"""
Phase 1 — The Gate Engine :: Generic Gate
=========================================
FORBIDDEN OUTPUT: the content of any SPECIFIC gate (that is data → Phase 3).
This defines HOW any gate runs, never WHICH preventer a given gate holds.

Contract:
    Gate := { id, preventer, license_question, precondition }
    run(gate, candidate) → (verdict, trace_entry)

STATUS: stub. To be filled in Phase 1.
"""
# TODO(Phase1): generic gate primitive: precondition → license_question → preventer → verdict
