# قرار تجميد الحزمة الدستوريّة — Constitution Freeze Gate

> **القرار الرسميّ:** تمّ إقرار الحزمة الدستوريّة للصندوق التحقيقيّ **v2.0**.

---

## المرجع الأعلى المعتمَد

لا يُبدأ تنفيذ Phase 0 التالي قبل تثبيت هذه الحزمة كمرجعٍ أعلى:

| الملفّ | الجنس | البصمة (sha256، أول 12) |
|--------|-------|--------------------------|
| `constitution.md` | ماذا يجب أن يكون | `1afdc449718a` |
| `development_plan.md` | كيف سيُبنى لاحقًا | `a3fa125a9f10` |
| `closure_criteria.md` | كيف تُختبَر السلامة | `09d9a458bb2e` |
| `implementation_status.md` | ماذا بُني فعلًا | **حيّ — لا يُجمَّد** (يُحدَّث مع كل تقدّم) |

> البصمات تُثبِّت النسخة المُقرَّة بعينها. أيّ تعديلٍ لاحقٍ يُغيّر البصمة، فيَظهر بوصفه تغييرًا
> واعيًا لا انزلاقًا صامتًا.

---

## القاعدة الحاكمة بعد التجميد

أيّ تنفيذٍ لاحق — بدءًا من `evidence.py` ثم `events.py` — يُعامَل بوصفه **تطبيقًا للدستور لا
تعديلًا عليه**، إلا إذا صدر **قرارٌ دستوريٌّ جديد** يُعلَن صراحةً ويُحدِّث الحزمة والبصمات.

---

## معيار بوابة التجميد (Constitution Freeze Gate)

اجتازت الحزمة البوابة بتحقّق الشروط الثلاثة:

- ✅ لا تعديلَ جوهريّ معلَّقٌ على الدستور.
- ✅ لا خلطَ بين الدستور والخطة والحالة (فُصلت الأجناس الثلاثة، وأُزيل الملفّ المختلط القديم).
- ✅ سُمّي المرجعُ الدستوريُّ المعتمَد صراحةً (الملفّات الأربعة أعلاه).

---

## ما بعد التجميد

الفصل التالي — حين يُقرَّر بدؤه — هو **Phase 0 التنفيذيّة**: تطبيق مذهب الدليل في
`evidence.py`، ثم مذهب الأثر والنسخة في `events.py`، تحت هذا الدستور المُجمَّد.

النقاش ينتقل حينها من «ما الذي يجب أن يكون؟» إلى «كيف نكتبه؟» — وهو انتقالٌ لا يبدأ إلا بقرارٍ
صريحٍ منك.

---

**الحالة:** 🔒 مُجمَّدة. بانتظار قرار بدء Phase 0 التنفيذيّة.

---

## سجلّ التعديلات بعد التجميد (Post-Freeze Changelog)

كلّ تغييرٍ في بصمةٍ يُدوَّن هنا صراحةً — تغييرٌ واعٍ لا انزلاقٌ صامت.

| التاريخ | الملفّ | البصمة القديمة | الجديدة | الطبيعة | السبب |
|---------|--------|----------------|---------|---------|-------|
| بعد الإقرار | `development_plan.md` | `8a707d866955` | `a3fa125a9f10` | تخطيطيّ (لا دستوريّ) | إضافة قسم «أفق التوسّع بعد Phase 2 — حزم التحقيق التسع» |

> **`constitution.md` لم يتغيّر** (`1afdc449718a` ثابتة). الخلاصة أُفرِدت في
> `PROJECT_PREAMBLE.md` مدخلًا قارئًا غير مُلزِم، فلم تُكسَر البصمات الدستوريّة.

---

## مكوّنات مجاورة (Adjacent Components — لا تمسّ الحزمة المُجمَّدة)

تُسجَّل هنا الدساتير المستقلّة لمكوّناتٍ **تقع خارج الصندوق**، فلا تعدّل حزمته ولا بصماته.

| المكوّن | الملفّ | موضعه | الأثر على البصمات المُجمَّدة |
|---------|--------|--------|------------------------------|
| مِهْيَع الدخول | `ENTRANCE_ADAPTER_CONSTITUTION.md` | **قبل** الصندوق | لا شيء — دستور الصندوق `1afdc449718a` ثابت |

> **الجملة الحاكمة للمِهْيَع:** «يُحضّر المرشّحات، ولا يُرخّص الأحكام.»
> حدُّه الأعلى `candidate` / `deferred_to_verifier`؛ ولا يقع `licensed` إلا داخل الصندوق.
> إضافةُ هذا الدستور **لم تُغيّر أيّ بصمةٍ مُجمَّدة** — تحقّقٌ مؤكَّد.

---

## سجلّ مراجعةٍ وتصحيح (Review & Reconciliation)

مراجعةٌ للتناقضات بين الوثائق والكود، وتصحيحها صراحةً:

| البند | ما وُجد | التصحيح |
|-------|---------|---------|
| تقرير الحالة قديم | يذكر 6 اختبارات وPhaseٍ١ «لم يبدأ»، ولا يذكر المِهْيَع | حُدِّث `implementation_status.md` ليعكس الواقع: 21 اختبارًا (18+3)، بوّابة قادح مُنفَّذة، المِهْيَع مُنفَّذ |
| بصمة تقرير الحالة | كانت مُجمَّدةً (`07ed97e7d6d1`) لوثيقةٍ **حيّةٍ بطبعها** | صُحِّح الخطأ التصميميّ: تقرير الحالة **لا يُجمَّد**؛ أُزيلت بصمته من جدول التجميد |
| اختبار مزدوج | `test_hadha_blocked_by_qadih` مؤجَّلٌ في acceptance بينما ناجحٌ في engine | وُضِّح: نسخة engine تُثبت البوّابة المفردة (ناجحة)؛ نسخة acceptance تُثبت النزول الكامل (مؤجَّلة) — اختباران مختلفان |
| انحراف Phase 1 | بُنيت `qadih_gate.py` (بوّابة بعينها) قبل المحرك العامّ، خلافًا لترتيب الخطة | دُوِّن الانحراف صراحةً في تقرير الحالة؛ يُعاد لاحقًا صوغُ البوّابة بيانات في المحرك العامّ |

> **البصمات المُجمَّدة الثابتة لم تتغيّر:** `constitution.md` = `1afdc449718a`،
> `closure_criteria.md` = `09d9a458bb2e`، `development_plan.md` = `a3fa125a9f10`.
> التغيير اقتصر على الوثيقة الحيّة (تقرير الحالة)، وهي بطبعها غير مُجمَّدة.
